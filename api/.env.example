PORT=3000

# PostgreSQL
PGHOST=localhost
PGPORT=5432
PGUSER=postgres
PGPASSWORD=
PGDATABASE=cafeteria

# Genera uno real con: node -e "console.log(require('crypto').randomBytes(48).toString('hex'))"
JWT_SECRET=

# Dominio del frontend (Cliente/Caja/Barista/Admin) que puede llamar a la API.
# En producción, NO usar "*".
CORS_ORIGIN=*
